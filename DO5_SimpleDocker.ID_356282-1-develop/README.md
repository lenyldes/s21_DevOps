# Контейнеризация сервисов, FastCGI на C, безопасность Dockle и Docker Compose (Service Containerization, FastCGI C Server, Dockle Security & Docker Compose)

![Docker](https://img.shields.io/badge/Docker-2496ED?style=flat&logo=docker&logoColor=white)
![Docker Compose](https://img.shields.io/badge/Docker_Compose-2496ED?style=flat&logo=docker&logoColor=white)
![Nginx](https://img.shields.io/badge/Nginx-009639?style=flat&logo=nginx&logoColor=white)
![FastCGI C](https://img.shields.io/badge/FastCGI-C_Language-blue?style=flat&logo=c&logoColor=white)
![Dockle](https://img.shields.io/badge/Dockle-Security_Audit-green?style=flat)
![Status](https://img.shields.io/badge/Status-Completed-success?style=flat)

Проект представляет собой комплексное практическое исследование и реализацию полного цикла контейнеризации серверных веб-приложений. В рамках работы выполнен переход от базового управления образами и контейнерами через Docker CLI к разработке собственного микросервиса на языке Си с интерфейсом FastCGI, созданию защищенного кастомного Docker-образа в соответствии с рекомендациями CIS Docker Benchmark, аудиту безопасности через утилиту Dockle и развертыванию отказоустойчивой мультиконтейнерной инфраструктуры с двухуровневым обратным проксированием (Reverse Proxy) средствами Docker Compose.

---

## О проекте

В современной DevOps-инженерии контейнеризация является базовым стандартом изоляции сред выполнения, гарантирующим переносимость, воспроизводимость и детерминированность поведения сервисов на всех этапах жизненного цикла ПО (разработка, тестирование, эксплуатация). Данный проект решает ключевые архитектурные и инфраструктурные задачи:

1. **Изоляция и стандартизация сред (Containerization Paradigm)**:
   Отказ от ручной конфигурации хост-систем в пользу переносимых изолированных контейнеров OCI (Open Container Initiative). Это исключает проблемы конфликта версий системных библиотек, упрощает миграцию сервисов между серверами и обеспечивает унифицированный способ сборки и доставки приложений.

2. **Интеграция Nginx с FastCGI-сервером на языке C**:
   В отличие от устаревшего протокола CGI (Common Gateway Interface), порождающего отдельный процесс операционной системы на каждый поступающий HTTP-запрос, протокол FastCGI сохраняет пул постоянно работающих процессов-демонов. В проекте реализован высокопроизводительный FastCGI-сервер на чистом Си (`server.c`), обрабатывающий входящие TCP-подключения через менеджер процессов `spawn-fcgi` и взаимодействующий с веб-сервером Nginx по протоколу FastCGI через локальный сокет.

3. **Соответствие стандартам безопасности (Container Hardening & CIS Docker Benchmark)**:
   Стандартные контейнеры часто запускаются с правами суперпользователя `root`, что создает критический вектор компрометации хоста в случае побега из контейнера (container breakout). В проекте реализован защищенный Dockerfile: все рабочие процессы Nginx и скомпилированного FastCGI-сервера функционируют под непривилегированным пользователем `nginx`, каталоги кэша и PID-файлов перенастроены по правам доступа, а кэш пакетного менеджера очищается на этапе сборки.

4. **Мультиконтейнерная оркестрация и декомпозиция (Microservices via Docker Compose)**:
   Разделение ответственности между фронтенд-прокси и внутренним сервисом приложений. Публичный периметр принимает запросы на стандартном HTTP-порту 80 и проксирует их во внутреннюю изолированную сеть Docker Bridge, где функционирует защищенный FastCGI-сервер, закрытый от прямого доступа из глобальной сети.

---

## Ключевые навыки и освоенные концепции

- **Управление жизненным циклом контейнеров через Docker CLI**:
  - Загрузка, инспекция и тегирование официальных образов (`docker pull`, `docker images`, `docker tag`).
  - Фоновый запуск контейнеров в detached-режиме (`docker run -d`), остановка с корректной отправкой сигнала `SIGTERM` (`docker stop`), перезапуск (`docker restart`) и удаление контейнеров (`docker rm`).
  - Глубокий аудит состояния контейнеров через `docker inspect`: фильтрация IP-адресов, структуры привязки портов (`NetworkSettings.Ports`) и анализ размера корневой файловой системы контейнера (`SizeRootFs`) с флагом `-s`.
  - Выполнение команд внутри работающего контейнера без его остановки через `docker exec` (интерактивные сессии bash и передача команд на лету).
  - Снапшотинг и миграция данных: экспорт файловой системы контейнера в плоский архив (`docker export`) и воссоздание базового образа через `docker import` с ручным определением директив запуска.

- **Сетевой маппинг и монтирование томов (Volume & Port Mapping)**:
  - Проброс сетевых портов с хоста в изолированное пространство сетевого стека контейнера (`-p <host_port>:<container_port>`).
  - Применение механизма bind-mount (`-v $(pwd)/...:/etc/nginx/...`) для горячего проброса конфигурационных файлов без пересборки Docker-образа.
  - Бесшовная перезагрузка конфигурации веб-сервера без обрыва клиентских соединений с помощью `nginx -s reload` (graceful reload).

- **Спецификация FastCGI и программирование на C**:
  - Работа с библиотекой FastCGI (`libfcgi-dev`) и системным заголовочным файлом `<fcgiapp.h>`.
  - Инициализация структуры запроса (`FCGX_InitRequest`) и организация неблокирующего цикла обработки событий (`FCGX_Accept_r`).
  - Формирование валидных HTTP-заголовков (`Content-Type: text/html\n\n`) и тела ответа через выводной поток `req.out` функции `FCGX_FPrintF`.
  - Управление сокетами и форками приложения через утилиту `spawn-fcgi` с привязкой к локальному TCP-порту `8080`.

- **Написание оптимизированных и защищенных Dockerfile**:
  - Минимизация слоев файловой системы OverlayFS за счет объединения связанных системных вызовов в единую инструкцию `RUN` через оператор `&&`.
  - Сборка бинарного файла Си внутри сборочного слоя без необходимости сторонних внешних скриптов.
  - Устранение разрастания размера образов путем обязательной очистки кэша пакетного менеджера `rm -rf /var/lib/apt/lists/*` в том же слое, где производилась установка зависимостей.
  - Конфигурирование PID 1 процесса и предотвращение фонового перехода Nginx через запуск на переднем плане (`daemon off;`).

- **Аудит безопасности контейнеров с линтером Dockle**:
  - Изучение и применение требований отраслевого стандарта CIS Docker Benchmark.
  - Устранение уязвимости **CIS-DI-0005** (запуск от имени root): создание условий для перехода на непривилегированного системного пользователя `USER nginx`, включая рекурсивную смену владельца директорий `/var/cache/nginx` и `/run`.
  - Устранение замечания **DKL-DI-0006**: валидация отсутствия временных индексов репозиториев apt.
  - Контроль прав доступа на исполняемые файлы (`chmod +x /app/start.sh`).

- **Оркестрация мультиконтейнерных приложений через Docker Compose**:
  - Декларативное описание распределенной инфраструктуры в файле `docker-compose.yml`.
  - Создание изолированной пользовательской сети типа `bridge` (`part_6`) со встроенным механизмом DNS-резолвинга (Service Discovery по доменным именам сервисов).
  - Настройка порядка инициализации зависимостей с помощью директивы `depends_on`.
  - Разделение внешнего Reverse Proxy и бэкенд-сервера для обеспечения сетевой изоляции.

---

## Архитектура и стек технологий

Инфраструктура проекта построена по классической двухуровневой микросервисной схеме с изоляцией внутренних компонентов:

1. **Уровень 1: Frontend Reverse Proxy (`proxy`)**:
   - Официальный образ `nginx:latest`.
   - Принимает входящий клиентский трафик на внешнем порту хост-машины `80` (проброс `80:8080` контейнера).
   - Выполняет роль границы периметра (Ingress). Перенаправляет все входящие запросы во внутреннюю подсеть Docker на узел `http://server:81` с помощью директивы `proxy_pass`.
   - Конфигурация подменяется через механизм bind-mount из `./part6_nginx/nginx.conf`.

2. **Уровень 2: Application Web Server (`server`)**:
   - Кастомный защищенный образ, собираемый из `./Dockerfile`.
   - Функционирует строго под непривилегированным пользователем `nginx`.
   - Слушает входящие соединения на внутреннем порту `81` (порт не публикуется на хост, доступен только внутри сети `part_6`).
   - Маршрут `/`: проксирует запросы на локальный FastCGI-демон `127.0.0.1:8080` (`fastcgi_pass 127.0.0.1:8080; include /etc/nginx/fastcgi_params;`).
   - Маршрут `/status`: возвращает страницу внутренней телеметрии Nginx (`stub_status`), отображающую число активных соединений, обработанных запросов и состояние очередей чтения/записи.

3. **Уровень 3: FastCGI C Daemon**:
   - Исполняемый файл `/app/server`, скомпилированный из исходного кода на C (`server.c`).
   - Запускается менеджером процессов `spawn-fcgi` на локальном адресе `127.0.0.1:8080`.
   - Формирует ответ с заголовком `Content-Type: text/html` и строкой `Hello, World!`.

### ASCII-схема сетевого взаимодействия и потоков трафика

```text
+---------------------------------------------------------------------------------------+
|                                  ХОСТ-МАШИНА (HOST)                                   |
|                                                                                       |
|   HTTP-клиент (curl / Web Browser)                                                    |
|        |                                                                              |
|        | Запрос: GET http://localhost:80/                                             |
|        v                                                                              |
|   [Порт хоста 80]                                                                     |
+--------|------------------------------------------------------------------------------+
         | Проброс портов (80:8080)
         v
+---------------------------------------------------------------------------------------+
|                             DOCKER BRIDGE NETWORK: part_6                             |
|                                                                                       |
|  +---------------------------------------------------------------------------------+  |
|  | СЕРВИС 1: proxy (Frontend Reverse Proxy Container)                              |  |
|  |                                                                                 |  |
|  | Входной порт: 8080                                                              |  |
|  | Конфигурация: /etc/nginx/nginx.conf (монтируется из ./part6_nginx/nginx.conf)    |  |
|  | Директива маршрутизации:                                                         |  |
|  |   location / {                                                                  |  |
|  |       proxy_pass http://server:81;                                              |  |
|  |   }                                                                             |  |
|  +---------------------------------------|-----------------------------------------+  |
|                                          |                                            |
|                                          | Внутренний HTTP-трафик                     |
|                                          | DNS Service Discovery: server -> IP:81     |
|                                          v                                            |
|  +---------------------------------------------------------------------------------+  |
|  | СЕРВИС 2: server (Backend Application Container / Non-root User: nginx)         |  |
|  |                                                                                 |  |
|  | [Внутренний порт 81]                                                            |  |
|  |   |                                                                             |  |
|  |   v                                                                             |  |
|  | Nginx Web Server (/etc/nginx/nginx.conf)                                        |  |
|  |   |-- location /status { stub_status; }   --> Метрики активных соединений       |  |
|  |   +-- location / {                                                              |  |
|  |           fastcgi_pass 127.0.0.1:8080;                                          |  |
|  |           include /etc/nginx/fastcgi_params;                                    |  |
|  |       }                                                                         |  |
|  |                                |                                                |  |
|  |                                | Протокол FastCGI (Loopback IPC / TCP)          |  |
|  |                                v                                                |  |
|  |       FastCGI Daemon (/app/server, C-бинарник, управляемый spawn-fcgi)          |  |
|  |       Адрес прослушивания: 127.0.0.1:8080                                       |  |
|  |       Ответ: "Content-Type: text/html\n\nHello, World!"                         |  |
|  +---------------------------------------------------------------------------------+  |
|                                                                                       |
+---------------------------------------------------------------------------------------+
```

### Стек технологий

| Категория | Технология / Инструмент | Назначение |
| :--- | :--- | :--- |
| Базовая платформа | Docker Engine, OCI | Контейнеризация и управление рантаймом |
| Оркестрация | Docker Compose v2 | Декларативное развертывание мультиконтейнерного стека |
| Веб-сервер / Прокси | Nginx (Latest) | Обратный прокси, терминализация FastCGI, модуль stub_status |
| Язык бэкенда | C (GCC) | Разработка легковесного FastCGI микросервиса |
| Библиотеки FastCGI | libfcgi-dev, spawn-fcgi | Обработка протокола FastCGI и запуск фонового процесса |
| Безопасность | Dockle | Статический анализ безопасности образов (CIS Docker Benchmark) |
| Тестирование | cURL, Bash | Автоматизация проверки эндпоинтов и сценариев сборки |

---

## Структура проекта и реализованные модули

Все исходные коды, конфигурации и отчетные материалы размещены в директории `src/`:

| Файл / Модуль | Назначение и функционал | Применяемые директивы / Технологии |
| :--- | :--- | :--- |
| [`./src/Dockerfile`](./src/Dockerfile) | Спецификация сборки защищенного кастомного Docker-образа с Nginx и FastCGI-сервером на C | `FROM nginx:latest`, `USER nginx`, сборка `gcc`, очистка `rm -rf /var/lib/apt/lists/*`, `chown -R nginx:nginx` |
| [`./src/docker-compose.yml`](./src/docker-compose.yml) | Сценарий оркестрации двухуровневой инфраструктуры с изоляцией сетей | `services`, `build`, `depends_on`, `ports: "80:8080"`, `volumes`, `networks: part_6 (bridge)` |
| [`./src/server/server.c`](./src/server/server.c) | Исходный код FastCGI веб-сервера на языке C, отдающего страницу Hello World | `<fcgiapp.h>`, `FCGX_Init`, `FCGX_InitRequest`, `FCGX_Accept_r`, `FCGX_FPrintF`, `FCGX_Finish_r` |
| [`./src/server/start.sh`](./src/server/start.sh) | Стартовый скрипт контейнера бэкенда: запуск демона FastCGI и мастер-процесса Nginx | `spawn-fcgi -p 8080 /app/server`, `nginx -g "daemon off;"` |
| [`./src/server/nginx/nginx.conf`](./src/server/nginx/nginx.conf) | Конфигурация Nginx для бэкенд-сервера: обработка FastCGI и сбор метрик | `listen 81`, `fastcgi_pass 127.0.0.1:8080`, `include /etc/nginx/fastcgi_params`, `location /status { stub_status; }` |
| [`./src/part6_nginx/nginx.conf`](./src/part6_nginx/nginx.conf) | Конфигурация Nginx для фронтенд Reverse Proxy контейнера | `listen 8080`, `location / { proxy_pass http://server:81; }` |
| [`./src/status.conf`](./src/status.conf) | Конфигурация виртуального хоста Nginx для проверки отдачи базовой статистики сервера | `listen 80`, `location /status { stub_status; }`, `root /usr/share/nginx/html` |
| [`./src/part_4.sh`](./src/part_4.sh) | Скрипт автоматизации сборки и запуска кастомного образа с внешним монтированием конфига | `docker build -t part_4:1.0 .`, `docker run -d -p 80:81 -v ...` |
| [`./src/server/info.txt`](./src/server/info.txt) | Инструкция по локальной компиляции, запуску C-демона и ручной проверке через cURL | `apt install libfcgi-dev spawn-fcgi gcc`, `gcc -lfcgi`, `spawn-fcgi -n` |
| [`./src/part_1.md`](./src/part_1.md) | Подробный отчет по базовым операциям Docker CLI (pull, run, inspect, stop, restart) | Документирование команд, разбор флагов `-d`, `-p`, `-s`, анализ `SizeRootFs` |
| [`./src/part_2.md`](./src/part_2.md) | Отчет по операциям с контейнером (exec, cp, reload, export/import, rmi -f, rm) | Документирование горячего копирования, graceful reload Nginx, экспорта в tarball |

---

## Инструкция по сборке, тестированию и запуску

Все команды выполняются в терминале Linux с установленным Docker Engine и Docker Compose.

### 1. Подготовка и переход в рабочий каталог

Перейдите в директорию `src`:

```bash
cd src
```

### 2. Сборка и запуск одиночного кастомного FastCGI-сервера (Part 4)

Соберите Docker-образ с помощью `docker build`:

```bash
docker build -t part_4:1.0 .
```

Запустите контейнер в фоновом режиме с пробросом порта 81 контейнера на порт 80 хоста:

```bash
docker run -d --name fastcgi_server -p 80:81 part_4:1.0
```

Проверьте доступность приложения и генерацию динамического ответа от бинарника Си:

```bash
curl http://localhost:80/
```
Ожидаемый ответ:
```html
Hello, World!
```

Проверьте доступность страницы статуса Nginx:

```bash
curl http://localhost:80/status
```
Ожидаемый ответ:
```text
Active connections: 1 
server accepts handled requests
 2 2 2 
Reading: 0 Writing: 1 Waiting: 0 
```

Остановите и удалите тестовый контейнер:

```bash
docker stop fastcgi_server && docker rm fastcgi_server
```

### 3. Аудит безопасности с помощью Dockle (Part 5)

Выполните статический аудит собранного образа на соответствие стандартам CIS Docker Benchmark:

```bash
dockle part_4:1.0
```

Если аудит запускается с официальным базовым образом, при необходимости передайте флаги игнорирования публичных GPG-ключей Nginx:

```bash
dockle -ak NGINX_GPGKEY -ak NGINX_GPGKEY_PATH part_4:1.0
```

Результат валидации подтверждает соблюдение ключевых правил:
- **[PASS] CIS-DI-0005**: Процессы контейнера запускаются от имени непривилегированного пользователя `nginx`, права на запись в служебные каталоги изолированы.
- **[PASS] DKL-DI-0006**: Индексы кэша APT (`/var/lib/apt/lists/*`) удалены в слое установки зависимостей.
- Отсутствуют критические уязвимости уровней `FATAL` и `WARN`.

### 4. Развертывание мультиконтейнерного стека через Docker Compose (Part 6)

Запустите мультиконтейнерную инфраструктуру с принудительной сборкой образов в фоновом режиме:

```bash
docker compose up -d --build
```

Проверьте статус запущенных сервисов:

```bash
docker compose ps
```

Ожидаемый вывод:
```text
NAME         IMAGE          COMMAND                  SERVICE   CREATED          STATUS          PORTS
src-proxy-1  nginx:latest   "/docker-entrypoint.…"   proxy     10 seconds ago   Up 9 seconds    0.0.0.0:80->8080/tcp, [::]:80->8080/tcp
src-server-1 src-server     "/app/start.sh"          server    10 seconds ago   Up 10 seconds   
```

### 5. Проверка функционирования сквозного проксирования

Отправьте HTTP-запрос на порт 80 хост-машины:

```bash
curl http://localhost:80/
```

Запрос проходит полный маршрут:
1. Хост (порт 80) ->
2. Контейнер `proxy` (порт 8080) ->
3. Bridge-сеть `part_6` ->
4. Контейнер `server` (порт 81) ->
5. Демон FastCGI C на сокете `127.0.0.1:8080`.

Ответ:
```html
Hello, World!
```

### 6. Остановка и очистка инфраструктуры

Для корректной остановки всех контейнеров, удаления внутренней сети и освобождения ресурсов выполните:

```bash
docker compose down
```
