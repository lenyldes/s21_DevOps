---
- name: Установка apache
  ansible.builtin.apt:
    name: 
      - apache2
    state: present
    update_cache: yes

- name: Запуск службы apache
  ansible.builtin.service:
    name: apache2
    state: started
    enabled: yes
