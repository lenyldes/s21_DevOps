# Основы операционной системы Linux и системного администрирования (UNIX/Linux OS Basics & Administration)

![OS: Ubuntu 20.04 LTS](https://img.shields.io/badge/OS-Ubuntu%2020.04%20LTS-E95420?style=flat-square&logo=ubuntu&logoColor=white)
![Shell: Bash](https://img.shields.io/badge/Shell-Bash-4EAA25?style=flat-square&logo=gnu-bash&logoColor=white)
![Init: Systemd](https://img.shields.io/badge/Init-Systemd-000000?style=flat-square)
![Service: SSH](https://img.shields.io/badge/Service-OpenSSH-231F20?style=flat-square&logo=ssh&logoColor=white)
![Status: Completed](https://img.shields.io/badge/Status-Completed-2ea44f?style=flat-square)

Инженерно-практическое руководство и техническая документация по развертыванию, базовой конфигурации, сетевой интеграции, харденингу и комплексному системному администрированию дистрибутива Ubuntu 20.04 LTS в серверном окружении. Проект охватывает полный жизненный цикл сопровождения Linux-системы: от инициализации сетевого стека и разграничения прав доступа до управления демонами через systemd, харденинга OpenSSH, планирования фоновых заданий в CRON, сбора метрик производительности и проведения низкоуровневого аудита файловых систем и системных журналов.

---

## О проекте

Эксплуатация серверных дистрибутивов семейства Linux в современной инфраструктуре требует строгого соблюдения практик стабильности, отказоустойчивости и информационной безопасности. Некорректная начальная настройка привилегий, оставленные без контроля стандартные порты сервисов, отсутствие синхронизации времени и бесконтрольное заполнение накопителей приводят к деградации сервисов и компрометации контура.

В рамках настоящего проекта решен комплекс инженерных задач системного администрирования Linux:
- Управление жизненным циклом пользователей, группами и дискреционной моделью доступа (DAC), включая делегирование суперпользовательских привилегий через конфигурацию sudoers.
- Конфигурирование сетевых интерфейсов, стека TCP/IP, маршрутизации и статической адресации посредством декларативного генератора Netplan, а также управление динамической арендой адресов через DHCP.
- Обеспечение точной временной синхронизации узла через клиентскую подсистему `systemd-timesyncd` по протоколу NTP.
- Управление системными сервисами, демонами и таргетами с помощью менеджера системы и служб `systemd`.
- Харденинг удаленного доступа OpenSSH: миграция со стандартного TCP-порта 22 на выделенный порт 2022, конфигурирование трансляции портов (Port Forwarding) и аудит открытых сетевых сокетов.
- Мониторинг вычислительных ресурсов (CPU, RAM, Swap, Load Average) и профилирование процессов с помощью утилит `top` и `htop`.
- Анализ блочных устройств, разметки MBR/GPT и файла подкачки через `fdisk` и `swapon`.
- Аудит емкости файловых систем и каталогов с применением инструментов `df`, `du` и интерактивного ncurses-анализатора `ncdu`.
- Централизованный аудит безопасности и мониторинг событий через системные журналы `/var/log/auth.log` и `/var/log/syslog`.
- Автоматизация регламентного обслуживания через планировщик периодических задач `cron` с широковещательным оповещением терминалов.

Подробный отчет о выполненных конфигурациях и верификационные материалы представлены в документе [src/D01_Linux.md](./src/D01_Linux.md).

---

## Ключевые навыки и освоенные концепции

В ходе реализации проекта освоены и применены на практике следующие ключевые компетенции (Hard Skills):

- Разметка блочных устройств и управление файловыми системами:
  - Низкоуровневый аудит структуры дисковых накопителей с помощью `fdisk` (анализ секторов, геометрии диска `/dev/sda` объемом 25 GiB, типов разделов).
  - Управление виртуальной памятью подкачки (`swap` объемом 2 GiB) и верификация через `swapon --show`.
  - Мониторинг емкости смонтированных файловых систем `ext4` через утилиту `df` (флаги `-T`, `-h`), контроль утилизации дискового пространства и inode.
  - Профайлинг дискового пространства каталогов (`/home`, `/var`, `/var/log`) с помощью `du` (флаги `-bh` для побайтового и человеко-читаемого вывода).
  - Интерактивная терминальная навигация и обнаружение избыточных файлов с помощью ncurses-утилиты `ncdu`.

- Управление демонами и инициализация системы (systemd):
  - Контроль жизненного цикла сервисов через `systemctl` (`status`, `start`, `stop`, `restart`, `enable`, `is-enabled`).
  - Развертывание и конфигурирование NTP-клиента `systemd-timesyncd` через `/etc/systemd/timesyncd.conf`.
  - Управление часовыми поясами и состоянием синхронизации времени с помощью `timedatectl` (`set-timezone Europe/Moscow`, верификация параметра `NTPSynchronized=yes`).

- Сетевое администрирование и сетевой стек:
  - Диагностика и инспекция физических и виртуальных сетевых интерфейсов (`lo`, `enp0s3`) с помощью `ip link`, `ip addr` и `ifconfig`.
  - Управление динамической конфигурацией через протокол DHCP с помощью клиента `dhclient` (освобождение аренды `-r`, повторный запрос).
  - Инспекция таблицы маршрутизации ядра (`ip route`), шлюза по умолчанию (default gateway) и внешнего IP через HTTP API (`curl ifconfig.me`).
  - Декларативное управление сетью через YAML-манифесты Netplan (`/etc/netplan/00-installer-config.yaml`) и применение конфигурации через `netplan apply`.
  - Диагностика сетевой связности и разрешения доменных имен через ICMP (`ping 1.1.1.1`, `ping ya.ru`).

- Безопасность, разграничение прав и SSHD Hardening:
  - Создание учетных записей (`adduser`), добавление в группы (`usermod -aG`), разбор структуры записей базы данных пользователей `/etc/passwd` и групп `/etc/group`.
  - Настройка модели привилегированного доступа через `sudo` и добавление пользователей в группу `sudo` для выполнения административных задач.
  - Укрепление безопасности демона OpenSSH (`sshd`): модификация конфигурационного файла `/etc/ssh/sshd_config`, перевод слушающего сокета на нестандартный порт 2022.
  - Аудит запущенных процессов через `ps aux | grep sshd` и анализ сетевых сокетов в состоянии LISTEN через `netstat -tan`.

- Системный мониторинг и управление процессами:
  - Сбор метрик производительности в реальном времени с помощью `top`: Load Average (1, 5, 15 минут), распределение процессорных состояний, использование физической и виртуальной памяти.
  - Расширенная интерактивная диагностика с помощью `htop`: сортировка процессов по PID, `%CPU`, `%MEM`, `TIME`, фильтрация и контекстный поиск по процессам `sshd` и `syslog`, настройка информационной панели (hostname, clock, uptime).

- Системный аудит, логирование и автоматизация:
  - Проверка и модификация прав доступа к файлам журналов (`ls -l`, `chmod +r`).
  - Аудит журнала безопасности `/var/log/auth.log`: идентификация успешных сессий входа, учетных записей и методов авторизации (`sshd:session`).
  - Анализ системного журнала `/var/log/syslog` и журнала подсистемы systemd (`journalctl`): отслеживание жизненного цикла демонов после рестарта, фильтрация по юнитам.
  - Конфигурирование планировщика периодических задач `cron` через `crontab -e`, синтаксис расписания `*/2 * * * *`, широковещательная рассылка уведомлений через `wall` и валидация выполнения заданий по записям в syslog.

- Консольные редакторы:
  - Эффективная работа в текстовых редакторах VIM (командный режим, режим вставки, сохранение и выход `:wq`, принудительный выход `:q!`, поиск `:\` и подстановка `:$s`), GNU nano и JOE.

---

## Архитектура и стек технологий

Архитектурно серверный стенд базируется на классической многоуровневой модели операционных систем семейства UNIX, где четко разграничены пространство пользователя (User Space), пространство ядра (Kernel Space) и уровень виртуализации оборудования.

```text
+-------------------------------------------------------------------------------+
|                             USER SPACE (ПРИЛОЖЕНИЯ)                           |
|  +-------------------------------------------------------------------------+  |
|  | Консольный инструментарий: bash, fdisk, df, du, ncdu, top, htop, vi, nano|  |
|  +-------------------------------------------------------------------------+  |
|  | Фоновые демоны и службы: sshd (Port 2022), systemd-timesyncd (NTP), cron|  |
|  +-------------------------------------------------------------------------+  |
|  | Аутентификация и контроль доступа: PAM (/etc/pam.d), sudo (/etc/sudoers)|  |
|  +-------------------------------------------------------------------------+  |
+-------------------------------------------------------------------------------+
                                      |
                           Системные вызовы (Syscalls)
                                      v
+-------------------------------------------------------------------------------+
|                           SYSTEMD & CORE SUBSYSTEMS                           |
|  +---------------------+  +----------------------+  +----------------------+  |
|  |  systemd (PID 1)    |  |  journald / rsyslog  |  |  Netplan / Networkd  |  |
|  |  (Unit Management)  |  |  (/var/log/syslog,   |  |  (/etc/netplan/*.yaml|  |
|  |  cgroups, targets   |  |   /var/log/auth.log) |  |   DHCP / Static IP)  |  |
|  +---------------------+  +----------------------+  +----------------------+  |
+-------------------------------------------------------------------------------+
                                      |
                                      v
+-------------------------------------------------------------------------------+
|                            KERNEL SPACE (ЯДРО LINUX)                          |
|  +-------------------------------------+  +--------------------------------+  |
|  |        Virtual File System (VFS)    |  |       Сетевой стек TCP/IP      |  |
|  |   ext4, /proc, /sys, swap management|  |   Netfilter, Sockets, Routes   |  |
|  +-------------------------------------+  +--------------------------------+  |
|  |       Диспетчер процессов и CPU     |  |       Управление памятью       |  |
|  |   Process Scheduler, Signals        |  |   Virtual Memory, Swapping     |  |
|  +-------------------------------------+  +--------------------------------+  |
+-------------------------------------------------------------------------------+
                                      |
                                      v
+-------------------------------------------------------------------------------+
|                        HARDWARE / VIRTUALIZATION LAYER                        |
|  +-------------------------+  +--------------------+  +--------------------+  |
|  |  Блочные накопители     |  | Сетевые адаптеры   |  | CPU / Virtual RAM  |  |
|  |  /dev/sda (25 GiB VDI)  |  | enp0s3 (NAT/Bridge)|  | 1-2 vCPU, 2 GiB    |  |
|  +-------------------------+  +--------------------+  +--------------------+  |
+-------------------------------------------------------------------------------+
```

### Системные подсистемы и компоненты стека

1. Подсистема VFS и блочные накопители:
   - Физический/виртуальный диск `/dev/sda` объемом 25 GiB, разбитый на системные разделы под управлением файловой системы `ext4`.
   - Выделенный раздел подкачки swap размером 2 GiB для предотвращения Out-Of-Memory (OOM) состояний.
   - Псевдофайловые системы `/proc` и `/sys` для динамического извлечения метрик ядра и аппаратных счетчиков.

2. Диспетчер системы и служб systemd (PID 1):
   - Управление cgroups, деревом процессов и таргетами (multi-user.target).
   - Контроль состояния служб `sshd.service`, `systemd-timesyncd.service` и `cron.service`.
   - Обеспечение автозапуска демонов при начальной инициализации ядра.

3. Сетевой стек Linux:
   - Абстракция сетевой конфигурации через Netplan с трансляцией настроек в бэкенд `systemd-networkd` / `NetworkManager`.
   - Протоколы IPv4, TCP/UDP, ICMP.
   - Маршрутизация трафика через шлюз по умолчанию и управление трансляцией сетевых адресов (NAT) на уровне гипервизора.

4. Подсистема PAM и безопасность:
   - Модули Pluggable Authentication Modules (PAM) для унифицированной аутентификации пользователей при входе через консоль и SSH.
   - Дискреционное управление доступом (DAC) на базе POSIX-прав (`rwx`) и правил `/etc/sudoers`.
   - Фиксация событий безопасности в подсистеме журналирования rsyslog/journald.

---

## Структура проекта и реализованные модули

Полный отчет о практических шагах, командах, конфигурациях и полученных экранных результатах зафиксирован в файле [src/D01_Linux.md](./src/D01_Linux.md). 

Сводная матрица выполненных модулей:

| Файл / Раздел | Назначение и функционал | Применяемые утилиты / Системные конфигурации |
| :--- | :--- | :--- |
| [`Part 1. Установка ОС`](./src/D01_Linux.md#part-1-установка-ос) | Развертывание базовой операционной системы Ubuntu 20.04 LTS и инспекция версии дистрибутива. | `cat`, `/etc/issue`, `/etc/os-release` |
| [`Part 2. Создание пользователя`](./src/D01_Linux.md#part-2-создание-пользователя) | Инициализация непривилегированной учетной записи, включение в системную группу adm, аудит базы учетных записей. | `adduser`, `usermod -aG`, `groups`, `su`, `/etc/passwd`, `/etc/group` |
| [`Part 3. Настройка сети ОС`](./src/D01_Linux.md#part-3-настройка-сети-ос) | Настройка hostname, установка часового пояса Europe/Moscow, управление DHCP-арендой, декларативная статическая адресация Netplan, аудит маршрутов и проверка ICMP. | `hostname`, `timedatectl`, `ip link`, `ip addr`, `ifconfig`, `dhclient`, `ip route`, `curl ifconfig.me`, `/etc/netplan/*.yaml`, `netplan apply`, `ping` |
| [`Part 4. Обновление ОС`](./src/D01_Linux.md#part-4-обновление-ос) | Синхронизация индексов репозиториев и обновление системных пакетов до актуальных версий. | `apt update`, `apt upgrade` |
| [`Part 5. Использование команды sudo`](./src/D01_Linux.md#part-5-использование-команды-sudo) | Делегирование прав суперпользователя непривилегированному пользователю, выполнение задач администрирования через механизм sudo. | `usermod -a -G sudo`, `sudo`, `/etc/sudoers`, `/etc/hostname` |
| [`Part 6. Установка и настройка службы времени`](./src/D01_Linux.md#part-6-установка-и-настройка-службы-времени) | Конфигурация клиента точного времени systemd-timesyncd по протоколу NTP, валидация синхронизации часов. | `systemctl status/restart`, `/etc/systemd/timesyncd.conf`, `timedatectl show` |
| [`Part 7. Установка и использование текстовых редакторов`](./src/D01_Linux.md#part-7-установка-и-использование-текстовых-редакторов) | Практическое освоение консольных редакторов VIM, GNU nano и JOE: навигация, режимы вставки, сохранение, принудительный выход, контекстный поиск и замена строк. | `vim`, `nano`, `joe`, `apt install joe` |
| [`Part 8. Установка и базовая настройка сервиса SSHD`](./src/D01_Linux.md#part-8-установка-и-базовая-настройка-сервиса-sshd) | Харденинг демона OpenSSH: миграция на порт 2022, настройка автозагрузки, аудит слушающих TCP-сокетов и проверка процессов. | `openssh-server`, `systemctl enable/status`, `/etc/ssh/sshd_config`, `netstat -tan`, `ps aux`, `grep` |
| [`Part 9. Установка и использование утилит top, htop`](./src/D01_Linux.md#part-9-установка-и-использование-утилит-top-htop) | Мониторинг системных метрик и профилирование процессов: Load Average, утилизация CPU и памяти, сортировка по PID/%CPU/%MEM/TIME, фильтрация по демонам. | `top`, `htop`, `/proc` |
| [`Part 10. Использование утилиты fdisk`](./src/D01_Linux.md#part-10-использование-утилиты-fdisk) | Низкоуровневый аудит структуры дисковых накопителей `/dev/sda` (25 GiB, 52428800 секторов), таблиц разделов и размера файла подкачки swap (2 GiB). | `fdisk -l`, `swapon --show`, `/proc/swaps` |
| [`Part 11. Использование утилиты df`](./src/D01_Linux.md#part-11-использование-утилиты-df) | Анализ утилизации дискового пространства корневой файловой системы ext4, расчет свободного и занятого объема в байтах и человеко-читаемом виде. | `df /`, `df -Th` |
| [`Part 12. Использование утилиты du`](./src/D01_Linux.md#part-12-использование-утилиты-du) | Оценка дискового пространства системных каталогов `/home`, `/var`, `/var/log` с детализацией в байтах и читаемых префиксах. | `du -bh` |
| [`Part 13. Установка и использование утилиты ncdu`](./src/D01_Linux.md#part-13-установка-и-использование-утилиты-ncdu) | Интерактивное сканирование и визуальный анализ структуры каталогов с наибольшим потреблением дискового пространства через ncurses. | `ncdu`, `apt install ncdu` |
| [`Part 14. Работа с системными журналами`](./src/D01_Linux.md#part-14-работа-с-системными-журналами) | Аудит прав доступа к логам, извлечение параметров успешных сессий SSH из `/var/log/auth.log`, мониторинг событий перезапуска сервисов в `/var/log/syslog`. | `ls -l`, `chmod +r`, `cat`, `/var/log/auth.log`, `/var/log/syslog`, `systemctl restart` |
| [`Part 15. Использование планировщика заданий CRON`](./src/D01_Linux.md#part-15-использование-планировщика-заданий-cron) | Автоматизация регламентных задач через cron: запуск команды каждые 2 минуты, рассылка широковещательных сообщений терминалам, аудит исполнения и очистка расписания. | `crontab -e`, `crontab -l`, `uptime`, `wall`, `/var/log/syslog` |

---

## Инструкция по сборке, тестированию и запуску

Для воспроизведения настроек и верификации инфраструктурного стенда выполните следующие пошаговые инструкции.

### 1. Требования к стенду и развертывание

- Гипервизор: VirtualBox 6.x/7.x, VMware Workstation или KVM/QEMU.
- Дистрибутив: Ubuntu 20.04 LTS Server (64-bit).
- Ресурсы виртуальной машины:
  - 1-2 vCPU;
  - 2048 MB RAM;
  - 25 GB VDI/QCOW2 виртуальный диск;
  - Сетевой адаптер: NAT с правилом проброса портов (Host Port: `2022`, Guest Port: `2022`, Protocol: `TCP`).

### 2. Инициализация и верификация сетевого стека

Проверка статуса сетевых интерфейсов:
```bash
ip link show
ip addr show enp0s3
```

Освобождение и повторный запрос IP-адреса по DHCP:
```bash
sudo dhclient -r enp0s3
sudo dhclient enp0s3
```

Проверка маршрута по умолчанию и внешнего IP-адреса:
```bash
ip route
curl -s ifconfig.me && echo ""
```

Применение статической конфигурации Netplan:
```bash
sudo netplan apply
ping -c 4 1.1.1.1
ping -c 4 ya.ru
```

### 3. Проверка системных сервисов (SSHD, Time Synchronization)

Проверка статуса и автозагрузки демона времени `systemd-timesyncd`:
```bash
systemctl status systemd-timesyncd
timedatectl show | grep NTPSynchronized
```

Проверка статуса и слушающего сокета службы OpenSSH на порту 2022:
```bash
systemctl status sshd
sudo netstat -tan | grep 2022
```

Подключение к виртуальной машине по защищенному протоколу SSH с хостовой системы:
```bash
ssh -p 2022 lenyldes@127.0.0.1
```

### 4. Мониторинг вычислительных ресурсов и процессов

Запуск консольного монитора top:
```bash
top
```

Запуск интерактивного монитора htop с фильтрацией:
```bash
htop
```

Анализ структуры дисков и виртуальной памяти подкачки:
```bash
sudo fdisk -l /dev/sda
sudo swapon --show
```

Анализ доступной емкости файловых систем:
```bash
df -Th /
```

Оценка размера системных директорий и логов:
```bash
sudo du -bh --max-depth=1 /var
sudo ncdu /var/log
```

### 5. Аудит системных журналов и планировщика CRON

Анализ последних событий аутентификации:
```bash
sudo tail -n 30 /var/log/auth.log
```

Просмотр сообщений системного журнала по демону SSH (syslog и journalctl):
```bash
sudo grep -i sshd /var/log/syslog | tail -n 20
sudo journalctl -u sshd -n 20 --no-pager
```

Проверка активных заданий в планировщике CRON:
```bash
crontab -l
```

Аудит срабатывания периодических задач в системном журнале:
```bash
sudo grep -i CRON /var/log/syslog | tail -n 20
```
