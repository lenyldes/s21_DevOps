# Комплексный мониторинг, генерация нагрузки и метрики Prometheus v2.0 (Linux System Monitoring, Stress Generation & Metrics v2.0)

![Bash](https://img.shields.io/badge/Language-Bash-4EAA25?style=flat&logo=gnu-bash&logoColor=white)
![Prometheus](https://img.shields.io/badge/Monitoring-Prometheus-E6522C?style=flat&logo=prometheus&logoColor=white)
![Grafana](https://img.shields.io/badge/Visualization-Grafana-F46800?style=flat&logo=grafana&logoColor=white)
![Docker](https://img.shields.io/badge/Containerization-Docker-2496ED?style=flat&logo=docker&logoColor=white)
![GoAccess](https://img.shields.io/badge/Analytics-GoAccess-008080?style=flat)
![Status](https://img.shields.io/badge/Status-Completed-success?style=flat)

Проект представляет собой комплекс инженерных скриптов и конфигураций на платформе GNU/Linux, ориентированный на моделирование критических сценариев эксплуатации дисковых подсистем, анализ телеметрии веб-серверов и построение промышленного контура наблюдаемости (observability). В репозитории реализованы алгоритмы детерминированной и вероятностной генерации файловых структур с жестким контролем доступного пространства (порог аварийной остановки 1 Гб), три независимые стратегии восстановления и очистки файловой системы, генератор синтетических журналов Nginx в формате Combined Log Format с потоковыми фильтрами на базе AWK, модуль генерации HTML-отчетов через контейнеризированный GoAccess, а также инфраструктурный стек сбора и визуализации метрик хоста на базе Prometheus, Node Exporter и Grafana в среде Docker Compose.

---

## О проекте

При эксплуатации высоконагруженных распределенных сервисов и облачной инфраструктуры ключевыми задачами SRE-инженера являются прогнозирование деградации подсистем хранения, быстрая локализация аномалий по журналам доступа и непрерывный мониторинг базовых аппаратных метрик. Проект решает указанные задачи через практическую реализацию следующих функциональных блоков:

1. **Моделирование высокой нагрузки на файловую систему (Storage Stress Testing):**
   - Контролируемое создание глубоких и разветвленных структур каталогов и файлов с заданными параметрами длин имен и объемов.
   - Имитация бесконтрольного разрастания данных на диске с вероятностным алгоритмом случайного обхода дерева каталогов хоста.
   - Выделение дискового пространства через прямой системный механизм быстрой аллокации блоков (`fallocate`) с отказоустойчивым переключением на усечение разреженных файлов (`truncate`).

2. **Предотвращение критического отказа подсистемы хранения (Disk Full Prevention):**
   - Непрерывный мониторинг доступного дискового пространства на корневом разделе (`/`) перед созданием каждого нового объекта.
   - Вычисление свободной емкости с точностью до килобайта через утилиту `df` для предотвращения ошибок округления.
   - Безопасное аварийное завершение работы генераторов при достижении порогового значения остатка в 1 Гб (1 048 576 Кб), исключающее отказ ядра, блокировку системных демонов и сбои журнала файловой системы.

3. **Многофакторная очистка и восстановление файловой системы (FS Sanitization):**
   - Устранение последствий стресс-тестирования без риска повреждения системных файлов и пользовательских данных.
   - Поддержка трех взаимодополняющих стратегий удаления артефактов: десериализация транзакционного журнала генерации, фильтрация по временному диапазону создания (`find -newermt`) и поиск по регулярным маскам имен и дат.
   - Встроенные правила защиты критических системных каталогов (`/bin`, `/sbin`, `/proc`, `/sys`, `/dev`, `/run`, `/boot`).

4. **Синтез и потоковый анализ журналов веб-сервера (Web Telemetry Processing):**
   - Генерация синтетических access-логов веб-сервера за 5 последовательных суток с соблюдением стандарта Nginx Combined Log Format.
   - Эмуляция распределения кодов ответов HTTP (2xx, 4xx, 5xx), разнообразных методов (GET, POST, PUT, PATCH, DELETE), реальных URL-маршрутов и 8 классов User-Agent.
   - Потоковая фильтрация, сортировка и агрегация данных журнала с использованием языка обработки текста `awk` без применения тяжелых внешних сред исполнения.
   - Развертывание легковесного интерактивного веб-дашборда через утилиту `GoAccess`, запущенную в изолированном Docker-контейнере.

5. **Комплексный мониторинг инфраструктуры в реальном времени (Host Observability):**
   - Развертывание оркестрируемого стека Node Exporter + Prometheus + Grafana в едином сетевом пространстве хоста (`network_mode: host`).
   - Сбор метрик с интервалом дискретизации 3 секунды для детальной фиксации кратковременных всплесков утилизации ресурсов.
   - Верификация поведения графиков утилизации CPU, распределения оперативной памяти, операций ввода-вывода (I/O) диска и сетевой пропускной способности под синтетической нагрузкой (`stress`, `clogger`, `iperf3`).

---

## Ключевые навыки и освоенные концепции

- **Комбинаторные алгоритмы генерации строк на Bash:** Разработка алгоритмов дополнения и модификации строк символов с сохранением исходного алфавитного порядка, динамическим наращиванием длины до нормативных требований (не менее 4 символов для Part 1 и не менее 5 символов для Part 2) и суффиксацией датой запуска в формате `_DDMMYY`.
- **Низкоуровневая работа с дисковым пространством:** Применение утилиты `fallocate` для предварительного выделения неразреженных блоков в файловых системах Linux без забивания диска нулями через `dd`, а также резервный фолбэк на `truncate`.
- **Потоковый разбор системных счетчиков:** Интеграция командного конвейера `df -k / | awk 'NR==2 {print $4}'` для детерминированного контроля остаточной емкости раздела в реальном времени.
- **Стратегии выборочного удаления в Linux:** Применение утилит `find`, `rm`, механизмов захвата групп регулярных выражений Bash (`BASH_REMATCH`) и временных модификаторов утилиты `find` (`-newermt`, `! -newermt`).
- **Спецификация Nginx Combined Log Format:** Генерация записей формата `$remote_addr - $remote_user [$time_local] "$request" $status $body_bytes_sent "$http_referer" "$http_user_agent"` со строго монотонно возрастающим таймстемпом в пределах каждых суток.
- **Текстовая аналитика на базе AWK:** Построение однострочных фильтров для извлечения полей логов, фильтрации регулярными выражениями кодов ошибок клиентской и серверной сторон (`$9 ~ /^[45][0-9]{2}$/`), объединения с утилитой `sort` для ранжирования (`sort -k 9 -n`) и дедупликации (`sort -u`).
- **Контейнеризация и интеграция CLI-утилит:** Конвейерная передача потоков данных `cat ... | docker run --rm -i ...` для генерации статических отчетов GoAccess без необходимости установки утилиты на хост.
- **Инфраструктура как код (IaC) с Docker Compose:** Декларативное описание сервисов телеметрии, настройка политик перезапуска (`unless-stopped`), механизмов проверки доступности (`healthcheck`) с зависимостями `condition: service_healthy`, а также маппинга системных директорий хоста (`/:/host:ro,rslave`).
- **Prometheus Time Series Database (TSDB):** Конфигурирование scrape-интервалов сбора данных, таргетов и экспортеров системных метрик.
- **Анализ телеметрии в Grafana:** Импорт и настройка дашбордов для детального отслеживания аппаратных ресурсов под воздействием утилит генерации стресс-нагрузки (`stress`) и пропускной способности сети (`iperf3`).

---

## Архитектура и стек технологий

Архитектура решения разделена на три взаимосвязанных функциональных контура: генераторно-очистительный контур файловой системы, конвейер обработки журналов веб-сервера и контур системного мониторинга.

### Архитектурная схема компонентов

```
+---------------------------------------------------------------------------------+
|               КОНТУР 1: МОДЕЛИРОВАНИЕ НАГРУЗКИ И ОЧИСТКА ФАЙЛОВОЙ СИСТЕМЫ        |
+---------------------------------------------------------------------------------+
|                                                                                 |
|   Параметры запуска                       Случайный обход FS (depth <= 100)     |
|          |                                               |                      |
|          v                                               v                      |
|   [src/01/generator.sh]                        [src/02/clogger.sh]              |
|   Генерация каталогов                          Вероятностный автомат:           |
|   и файлов N Кб                                35% create / 60% cd / 5% root    |
|          |                                               |                      |
|          +---> [Контроль емкости: check_free_space] <----+                      |
|                df -k / -> Порог 1 Гб (1048576 Кб)                               |
|                               |                                                 |
|                +--------------+--------------+                                  |
|                v                             v                                  |
|        Создание файлов              Запись транзакций                           |
|      (fallocate / truncate)         в generator.log / clogging.log              |
|                |                             |                                  |
|                +--------------+--------------+                                  |
|                               v                                                 |
|                   [src/03/ Очистка ФС]                                          |
|                   |-- clean_log.sh  (по журналу транзакций)                     |
|                   |-- clean_time.sh (по диапазону дат/времени find -newermt)    |
|                   \-- clean_mask.sh (по шаблону имени *_DDMMYY*)                |
+---------------------------------------------------------------------------------+

+---------------------------------------------------------------------------------+
|               КОНТУР 2: КОНВЕЙЕР СИНТЕЗА И АНАЛИЗА ЖУРНАЛОВ NGINX               |
+---------------------------------------------------------------------------------+
|                                                                                 |
|   [src/04/data.sh + generator.sh]                                               |
|   Синтез 5 файлов логов (100..1000 записей/день, Nginx Combined Log Format)     |
|                               |                                                 |
|                               v                                                 |
|                     [src/04/logs/nginx_*.log]                                   |
|                               |                                                 |
|               +---------------+---------------+                                 |
|               v                               v                                 |
|      [src/05/parser.sh]              [src/06/report.sh]                         |
|      Анализ через AWK & sort         cat nginx_*.log | docker run GoAccess      |
|      |-- Режим 1: Сортировка по коду          |                                 |
|      |-- Режим 2: Уникальные IP               v                                 |
|      |-- Режим 3: Ошибки 4xx/5xx        [report.html]                           |
|      \-- Режим 4: IP среди ошибок             |                                 |
|                                               v                                 |
|                                      python3 -m http.server (Порт 8080)         |
+---------------------------------------------------------------------------------+

+---------------------------------------------------------------------------------+
|               КОНТУР 3: КОНТУР НАБЛЮДАЕМОСТИ (OBSERVABILITY STACK)              |
+---------------------------------------------------------------------------------+
|                                                                                 |
|   Хостовая операционная система Linux (CPU, Memory, Disk I/O, Network)          |
|                               |                                                 |
|                               | Монтирование /:/host:ro,rslave                  |
|                               | network_mode: host                              |
|                               v                                                 |
|                   [Контейнер: node_exporter]                                    |
|                   Метрики хоста: http://localhost:9100/metrics                  |
|                               |                                                 |
|                               | Pull-запросы каждые 3 секунды                   |
|                               v                                                 |
|                   [Контейнер: prometheus]                                       |
|                   Хранилище временных рядов (TSDB): http://localhost:9090       |
|                               |                                                 |
|                               | PromQL запросы                                  |
|                               v                                                 |
|                   [Контейнер: grafana]                                          |
|                   Дашборды и визуализация: http://localhost:3000                |
|                               ^                                                 |
|                               |                                                 |
|   [Генераторы нагрузки]-------+                                                 |
|   |-- stress (CPU / RAM)                                                        |
|   |-- clogger.sh (Disk I/O / Capacity)                                          |
|   \-- iperf3 (Network bandwidth)                                                |
+---------------------------------------------------------------------------------+
```

### Стек технологий

- **Оболочка и системные утилиты:** GNU Bash 5+, AWK, Coreutils (`find`, `sort`, `df`, `date`, `cat`, `rm`, `wc`), `fallocate`, `truncate`.
- **Контейнеризация и оркестрация:** Docker Engine 24+, Docker Compose v2.
- **Мониторинг и метрики:** Prometheus, Prometheus Node Exporter, Grafana Enterprise.
- **Аналитика журналов:** GoAccess (Docker-образ `allinurl/goaccess`), встроенный модуль Python 3 `http.server`.
- **Нагрузочное тестирование:** `stress`, `iperf3`.

---

## Структура проекта и реализованные модули

В таблице ниже приведен полный перечень модулей, скриптов и конфигурационных файлов проекта с прямыми ссылками на исходный код.

| Файл / Модуль | Назначение и функционал | Применяемые инструменты / API |
| :--- | :--- | :--- |
| [`./src/01/main.sh`](./src/01/main.sh) | Точка входа генератора файлов и папок. Оркестрирует вызовы валидации входных аргументов и запускает алгоритм генерации. | Bash, source, условные операторы |
| [`./src/01/validator.sh`](./src/01/validator.sh) | Валидация 6 позиционных аргументов Part 1: проверка абсолютного пути, целочисленных счетчиков, алфавитных масок (до 7 символов имени, до 3 расширения) и размера файлов (до 100kb). | Bash Regex (`=~`), потоки ошибок |
| [`./src/01/generator.sh`](./src/01/generator.sh) | Создание каталогов и файлов с динамическим расширением базового имени (до >= 4 знаков), выделением места через `fallocate`/`truncate`, непрерывным контролем остатка места (порог 1 Гб) и логированием в `generator.log`. | `fallocate`, `truncate`, `df -k`, `awk`, `mkdir -p` |
| [`./src/02/main.sh`](./src/02/main.sh) | Точка входа скрипта засорения файловой системы. Фиксирует время начала и окончания выполнения, рассчитывает хронометраж и выводит итоговую статистику. | Bash, `date +%s`, арифметические вычисления |
| [`./src/02/validator.sh`](./src/02/validator.sh) | Валидация 3 позиционных параметров Part 2: список букв для папок, маска файлов и размер в мегабайтах (до 100Mb). | Bash Regex, проверка диапазонов |
| [`./src/02/clogger.sh`](./src/02/clogger.sh) | Ядро стресс-тестирования диска: вероятностный автомат обхода дерева файловой системы (глубина до 100), исключение системных папок, генерация 1-5 файлов на каталог, проверка лимита свободного места в 1 Гб и ведение журнала `clogging.log`. | `RANDOM`, `find -type d -writable`, `df -h`, `df -k`, `fallocate` |
| [`./src/03/main.sh`](./src/03/main.sh) | Точка входа подсистемы очистки файловой системы. Маршрутизирует выполнение по одному из трех выбранных методов очистки. | Bash case-switch, диспетчеризация модулей |
| [`./src/03/clean_log.sh`](./src/03/clean_log.sh) | Способ 1 очистки: парсинг файла журнала `clogging.log`, извлечение абсолютных путей через регулярные выражения и их каскадное удаление. | `BASH_REMATCH`, `rm -rf`, потоковое чтение `while read` |
| [`./src/03/clean_time.sh`](./src/03/clean_time.sh) | Способ 2 очистки: удаление файлов и директорий, созданных в заданный временной интервал (с точностью до минуты), с защитой системных разделов. | `find -newermt`, `date -d`, фильтрация по `_DDMMYY` |
| [`./src/03/clean_mask.sh`](./src/03/clean_mask.sh) | Способ 3 очистки: поиск и удаление объектов по маске имени или даты с фильтрацией системных каталогов. | `find -name`, регулярные выражения, `rm -rf` |
| [`./src/04/main.sh`](./src/04/main.sh) | Точка входа генератора логов Nginx. Проверяет аргументы и инициализирует генерацию 5 журналов в подкаталоге `logs/`. | Bash, координация модулей |
| [`./src/04/validator.sh`](./src/04/validator.sh) | Модуль проверки запуска Part 4 (гарантирует отсутствие посторонних аргументов командной строки). | Проверка `$# -eq 0` |
| [`./src/04/data.sh`](./src/04/data.sh) | Словари и наборы синтетических данных: 10 кодов состояний HTTP (200..503), HTTP-методы, типовые URL-пути и 8 классов User-Agent. | Массивы Bash (`ARRAY`) |
| [`./src/04/generator.sh`](./src/04/generator.sh) | Генератор синтетических записей Nginx Combined Log Format: случайная генерация IPv4, монотонный расчет меток времени в рамках суток (100..1000 записей в день). | Случайная адресация IPv4, расчет таймстемпов, форматирование даты |
| [`./src/05/main.sh`](./src/05/main.sh) | Точка входа анализатора логов Nginx. Проверяет аргументы, верифицирует наличие файлов журналов и запускает парсер. | Bash, проверка наличия артефактов |
| [`./src/05/validator.sh`](./src/05/validator.sh) | Валидатор Part 5: проверка допустимого режима обработки (1, 2, 3 или 4) и верификация наличия файлов `nginx_*.log`. | Bash Regex, проверка файлов через `find` |
| [`./src/05/parser.sh`](./src/05/parser.sh) | Реализация 4 режимов аналитики логов с помощью потоковых конвейеров AWK и Sort (сортировка по кодам, уникальные IP, фильтрация ошибок 4xx/5xx, уникальные IP с ошибками). | `awk`, `sort -k 9 -n`, `sort -u`, регулярные выражения |
| [`./src/06/main.sh`](./src/06/main.sh) | Точка входа для веб-анализатора GoAccess: проверяет окружение, запускает контейнеризированную сборку отчета и открывает локальный веб-сервер. | Bash, управление пайплайном визуализации |
| [`./src/06/validator.sh`](./src/06/validator.sh) | Проверка зависимостей Part 6: наличие утилит `docker` и `python3`, доступность сокета демона Docker, верификация наличия логов. | `command -v`, `docker info`, проверка статусов возврата |
| [`./src/06/report.sh`](./src/06/report.sh) | Пайплайн генерации статического HTML-отчета через официальный Docker-образ GoAccess и запуск встроенного HTTP-сервера Python на порту 8080. | `docker run --rm -i allinurl/goaccess`, `python3 -m http.server` |
| [`./src/07/main.sh`](./src/07/main.sh) | Точка входа инфраструктуры мониторинга. Валидирует Docker Compose, запускает сервисы в фоне, выполняет проверку состояния и выводит доступные URL. | Bash, `docker compose up -d`, `docker compose ps` |
| [`./src/07/validator.sh`](./src/07/validator.sh) | Проверка окружения для Part 7: доступность демона Docker, наличие плагина `docker compose`. | `docker compose version`, `docker info` |
| [`./src/07/docker-compose.yml`](./src/07/docker-compose.yml) | Декларативная спецификация стека мониторинга: сервисы `node_exporter`, `prometheus`, `grafana` с сетевым режимом host, health-check проверками и томами данных. | Docker Compose Specification, healthchecks, volume mapping |
| [`./src/07/prometheus.yml`](./src/07/prometheus.yml) | Конфигурация Prometheus: глобальный интервал сбора метрик `scrape_interval: 3s`, scrape-конфигурация таргета `localhost:9100`. | YAML, Prometheus Configuration Spec |
| [`./src/08/Screenshot_20260826_130349.png`](./src/08/Screenshot_20260826_130349.png) | Артефакт тестирования: снимок экрана готового дашборда Grafana (Node Exporter Full), подтверждающий корректность фиксации метрик под комплексной нагрузкой. | Графический отчет (2554x1231 PNG) |

---

## Инструкция по сборке, тестированию и запуску

### 1. Подготовка окружения

Для запуска скриптов и инфраструктурных модулей требуются следующие установленные пакеты:
- Операционная система: Linux (дистрибутивы на базе Debian/Ubuntu или RHEL/Fedora).
- Командный интерпретатор: GNU Bash версии 4.4+.
- Утилиты окружения: `coreutils`, `util-linux`, `awk`, `python3`.
- Контейнеризация: Docker Engine и плагин Docker Compose v2.
- Пакеты стресс-тестирования (для Part 7-8): `stress`, `iperf3`.

Предоставьте права на исполнение всем сценариям проекта:
```bash
chmod +x ./src/01/*.sh ./src/02/*.sh ./src/03/*.sh ./src/04/*.sh ./src/05/*.sh ./src/06/*.sh ./src/07/*.sh
```

---

### 2. Часть 1: Управляемая генерация структуры каталогов и файлов

Скрипт создает указанное количество директорий и файлов в заданной локации с гарантией сохранения свободного места (не менее 1 Гб на `/`).

Синтаксис вызова:
```bash
./src/01/main.sh <целевая_папка> <кол-во_папок> <буквы_папок> <кол-во_файлов> <буквы_файлов.расширение> <размер_файла>
```

Пример запуска (создание 4 папок по маске `az`, в каждой по 5 файлов размером 3 Кб):
```bash
./src/01/main.sh /opt/test 4 az 5 az.az 3kb
```

Результаты работы и список созданных объектов сохраняются в файл `./src/01/generator.log`.

---

### 3. Часть 2: Моделирование стресс-нагрузки на файловую систему

Скрипт производит обход файловой системы хоста, создавая папки и файлы заданного размера в случайных директориях до тех пор, пока свободное место на корневом разделе не снизится до порога в 1 Гб.

Синтаксис вызова:
```bash
./src/02/main.sh <буквы_папок> <буквы_файлов.расширение> <размер_файла>
```

Пример запуска (генерация файлов размером 3 Мб):
```bash
sudo ./src/02/main.sh az az.az 3Mb
```

По окончании работы скрипт выводит общее время выполнения, хронометраж и сохраняет полный лог в `./src/02/clogging.log`.

---

### 4. Часть 3: Очистка файловой системы после тестов

Подсистема очистки восстанавливает состояние системы одним из трех способов.

Синтаксис вызова:
```bash
./src/03/main.sh <метод> [параметры_метода]
```

#### Способ 1: Очистка по лог-файлу
Удаляет объекты, записанные в журнал транзакций Part 2 (по умолчанию `./src/02/clogging.log`):
```bash
sudo ./src/03/main.sh 1 ./src/02/clogging.log
```

#### Способ 2: Очистка по дате и времени создания
Удаляет объекты с суффиксом даты `_DDMMYY`, созданные в указанный интервал времени (с точностью до минуты):
```bash
sudo ./src/03/main.sh 2 "2026-08-27 07:00" "2026-08-27 08:30"
```

#### Способ 3: Очистка по маске имени
Удаляет объекты, соответствующие шаблону маски символов и даты:
```bash
sudo ./src/03/main.sh 3 "az_$(date +%d%m%y)"
```

---

### 5. Часть 4: Генерация журналов веб-сервера Nginx Combined

Генерация 5 файлов журналов с записями за 5 суток (сохраняются в `./src/04/logs/nginx_*.log`):
```bash
./src/04/main.sh
```

Каждый файл содержит от 100 до 1000 записей со строго возрастающими метками времени.

---

### 6. Часть 5: Потоковый анализ и фильтрация логов через AWK

Анализатор считывает сформированные логи Nginx и выполняет потоковую обработку в зависимости от выбранного режима.

Синтаксис вызова:
```bash
./src/05/main.sh <1|2|3|4>
```

- **Режим 1:** Вывод всех записей, отсортированных по коду ответа HTTP:
  ```bash
  ./src/05/main.sh 1
  ```
- **Режим 2:** Вывод всех уникальных IP-адресов клиентов:
  ```bash
  ./src/05/main.sh 2
  ```
- **Режим 3:** Вывод всех запросов с ошибками (коды ответа 4xx и 5xx):
  ```bash
  ./src/05/main.sh 3
  ```
- **Режим 4:** Вывод уникальных IP-адресов клиентов, среди запросов которых зафиксированы ошибки:
  ```bash
  ./src/05/main.sh 4
  ```

---

### 7. Часть 6: Интерактивная аналитика через GoAccess

Скрипт объединяет сгенерированные логи Nginx, передает их в официальный Docker-контейнер `allinurl/goaccess`, формирует статический файл отчета `report.html` и запускает веб-сервер.

Запуск:
```bash
./src/06/main.sh
```

После запуска отчет доступен в браузере по адресу:
- `http://localhost:8080/report.html`

Для остановки локального веб-сервера нажмите комбинацию клавиш `Ctrl+C`.

---

### 8. Части 7 и 8: Мониторинг Prometheus, Node Exporter и Grafana

#### Запуск стека мониторинга
Развертывание контейнеров в фоновом режиме через оркестратор:
```bash
./src/07/main.sh
```
Или напрямую через Docker Compose:
```bash
docker compose -f ./src/07/docker-compose.yml up -d
```

Проверка состояния сервисов:
```bash
docker compose -f ./src/07/docker-compose.yml ps
```

Доступ к веб-интерфейсам компонентов:
- **Prometheus UI:** `http://localhost:9090` (просмотр метрик и таргетов в разделе Status -> Targets).
- **Node Exporter raw metrics:** `http://localhost:9100/metrics` (сырые системные метрики ядра).
- **Grafana Dashboard:** `http://localhost:3000` (учетные данные по умолчанию: логин `admin`, пароль `admin`).

#### Настройка дашборда в Grafana
1. Перейдите в `Connections -> Data Sources` и добавьте источник данных `Prometheus` с URL `http://localhost:9090`.
2. Импортируйте готовый дашборд *Node Exporter Quickstart and Dashboard* (ID `13978` или `1860`).
3. Контролируйте ключевые показатели: утилизацию процессорных ядер (CPU Usage), использование оперативной памяти (RAM Available / Used), остаток дискового пространства и интенсивность дисковых операций ввода-вывода (Disk I/O).

#### Проведение нагрузочного тестирования и проверка телеметрии
1. **Нагрузка на процессор и оперативную память:**
   ```bash
   stress -c 2 -i 1 -m 1 --vm-bytes 32M -t 10s
   ```
2. **Нагрузка на подсистему дискового ввода-вывода:**
   ```bash
   sudo ./src/02/main.sh test test.log 10Mb
   ```
3. **Нагрузка на сетевой стек хоста (iperf3):**
   Запуск iperf3 в режиме сервера в отдельном терминале:
   ```bash
   iperf3 -s
   ```
   Генерация сетевого трафика в режиме клиента:
   ```bash
   iperf3 -c localhost -t 10
   ```
4. Фиксация изменений на панелях дашборда Grafana (результаты подтверждены снимком экрана в [`./src/08/Screenshot_20260826_130349.png`](./src/08/Screenshot_20260826_130349.png)).

#### Остановка стека мониторинга
После завершения сессии мониторинга сервисы останавливаются следующей командой:
```bash
docker compose -f ./src/07/docker-compose.yml down -v
```
